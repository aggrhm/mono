#!/usr/bin/env node

process.on('uncaughtException', (err) => {
  console.error(err);
  process.exit(1);
});

require('../dist/src/index.js').run( (process.env.INIT_CWD || process.cwd()), process.argv );
